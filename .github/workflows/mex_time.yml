name: Test running time concerning MEX

on:
  # Trigger the workflow on push or pull request
  push:
  #pull_request:  # DANGEROUS! MUST be disabled for self-hosted runners!
  # Trigger the workflow by cron. The default time zone of GitHub Actions is UTC.
  schedule:
      - cron: '0 19 * * *'
  # Trigger the workflow when it is manually triggered
  workflow_dispatch:

jobs:
  test:
    name: Test running time concerning MEX
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        bitness: [32, 64]

    steps:
      - name: Check out repository
        uses: actions/checkout@v2


      - name: Set up MATLAB
        uses: matlab-actions/setup-matlab@v1.1.0

      - name: Run script
        uses: matlab-actions/run-command@v1
        with:
          command: |
              tic;
              mex('-setup', '-v', 'C');
              fprintf('\n\n=======> Time for setting MEX up: %f\n\n', toc);
              tic
              mex(fullfile(matlabroot, 'extern', 'examples', 'refbook', 'timestwo.c'));
              fprintf('\n\n=======> Time for mexifying timestwo: %f\n\n', toc);
              tic
              for i = 1 : 100
                  timestwo(i);
              end
              fprintf('\n\n=======> Time for 100 runs of timestwo: %f\n\n', toc);
